= Remote Procedure Call (rpc)=

Explain why it is needed, what it is used for (np, np2, nt, sob, etc), bennifits of HTTP? touch on old dcirpc?


Since each service (np, np2, nt, sob) each sets up their own rpc client - we should include that in the config section of each service.


In this config - the nssock of the tool server is also accepting rpc connections.  Should we how to set up a separate nssock for rpc?
